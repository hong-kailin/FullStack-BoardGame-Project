# 🎮 璀璨宝石对决（Splendor Duel）在线版 — 课程大纲

> 本课程面向全栈零基础的学生，项目驱动，从零到一构建完整 Web 应用。

---

## 📚 总览

> ⚠️ 以下课时安排为预设计划，实际教学过程中会根据学习进度和理解情况动态调整（增删课时、调整顺序等），以实际教学为准。

| 阶段 | 名称 | 课时 | 核心产出 |
|------|------|------|----------|
| 一 | Monorepo 基建 | 2 课 | 项目骨架搭建完毕，两个包可互相引用 |
| 二 | 游戏核心逻辑（终端版） | 8 课 | 可在终端完整进行一局双人对战 |
| 三 | Web 界面（本地双人） | 10 课 | 浏览器中可玩的本地双人游戏 |
| 四 | 用户系统 | 6 课 | 注册、登录、游戏记录 |
| 五 | AI 对手 | 4 课 | 可在浏览器中与 AI 对战 |
| 六 | 在线联机 | 6 课 | 两人通过网络远程对战 |
| 七 | 部署上线 | 2 课 | 让全世界都能玩 |

**总计：约 38 课时**

---

# 阶段一：Monorepo 基建（2 课时）

### 第 1 课：认识项目结构
- 什么是 monorepo？和多个独立仓库的区别
- npm workspaces 是什么、为什么需要它
- Turborepo 是什么、解决了什么问题
- 创建根目录 `package.json`，配置 workspaces
- 初始化 Turborepo（`turbo.json`）

### 第 2 课：创建并连接子包
- 创建 `packages/core`（游戏逻辑包）
- 创建 `packages/web`（Web 应用包）
- 配置 TypeScript（`tsconfig.json`）
- 配置互相引用：`@splendor/core` 和 `@splendor/web`
- 验证：在 web 包中 import core 的代码并运行成功

---

# 阶段二：游戏核心逻辑 — 终端版（8 课时）

### 第 3 课：游戏规则概览 + 类型定义
- 讲解璀璨宝石对决的核心规则
- TypeScript 基础语法速览（变量、类型、接口）
- 定义游戏中的基础类型：宝石颜色、卡牌、玩家状态
- 类比：TS 的 `interface` ≈ C++ 的 `struct` / Python 的 `dataclass`

### 第 4 课：实现卡牌池
- 设计卡牌数据：等级、所需宝石、提供的声望点/宝石
- 卡牌数据写死在代码中（不引入 JSON 或数据库）
- 实现发牌函数：从牌堆随机抽取若干张展示

### 第 5 课：玩家操作 — 拿取宝石
- 实现玩家手牌、宝石持有量
- 实现"拿取 3 种不同颜色宝石各 1 个"操作
- 实现"拿取同种颜色宝石 2 个"操作
- 边界检查：宝石数量不能超过上限

### 第 6 课：玩家操作 — 购买卡牌
- 检查玩家宝石是否足够支付
- 扣除宝石、添加卡牌到玩家手牌
- 卡牌提供的宝石折扣生效

### 第 7 课：游戏状态管理
- 设计 `GameState` 数据结构
- 实现回合切换（玩家 1 → 玩家 2）
- 实现胜利条件检查（达到指定声望点数）

### 第 8 课：终端界面 v1 — 纯文本显示
- 用 `console.log` 显示当前游戏状态
- 用 `readline` 接收玩家输入
- 命令行显示：可用卡牌、玩家宝石、得分

### 第 9 课：终端界面 v2 — 操作交互
- 将命令行输入解析为操作（拿宝石 / 买卡）
- 错误处理：输入无效时的提示
- 完整的双人轮流操作流程

### 第 10 课：联调测试
- 完整跑通一局游戏
- 边界情况测试（宝石上限、分数胜利等）
- 代码重构：将类型定义抽离到独立文件

---

# 阶段三：Web 界面 — 本地双人（10 课时）

### 第 11 课：HTML + CSS 基础
- 从零写一个 HTML 页面
- CSS 基础：选择器、盒模型、Flexbox
- 制作一个简单的游戏界面布局
- 类比：HTML ≈ 骨架，CSS ≈ 衣服

### 第 12 课：JavaScript DOM 操作
- JS 基础：变量、函数、事件
- 获取 DOM 元素、绑定点击事件
- 用 JS 动态修改页面内容
- 类比：JS ≈ 大脑，控制行为

### 第 13 课：在浏览器中运行游戏逻辑
- 在 `packages/web` 中引入 `@splendor/core`
- 用 Webpack/Vite 打包（选择最简单的方案）
- 在浏览器控制台调用游戏函数

### 第 14 课：渲染游戏状态到页面
- 将卡牌渲染为 HTML 卡片
- 显示玩家宝石和得分
- 实现状态更新时重新渲染
- 痛点暴露：每次手动操作 DOM 很繁琐

### 第 15 课：实现点击操作
- 点击卡牌 → 购买
- 点击宝石堆 → 拿取宝石
- 点击"结束回合"按钮
- 代码开始变得混乱（事件绑定、状态同步）

### 第 16 课：引入 React — 为什么需要它
- 演示：用原生 JS 实现同样功能的代码量 vs React
- React 核心思想：组件化、声明式 UI、单向数据流
- 安装 React 到 web 包
- 重写最简单的组件（PlayerInfo）

### 第 17 课：React 组件化改造
- 将游戏界面拆分为组件：`Board`、`Card`、`PlayerHand`、`GemPile`
- Props 传递数据
- useState 管理组件状态

### 第 18 课：React 状态管理
- 将 GameState 提升到顶层组件
- 通过回调函数传递操作
- 对比：原生 JS 的痛点和 React 的解决方案

### 第 19 课：UI 美化
- CSS 进阶：动画、过渡
- 卡牌翻转动画、宝石选中效果
- 响应式布局适配不同屏幕

### 第 20 课：Web 版联调
- 完整的本地双人对战流程
- 发现并修复交互 bug
- 总结：从原生到 React 的心路历程

---

# 阶段四：用户系统（6 课时）

### 第 21 课：Node.js 原生 HTTP 服务器
- Node.js 基础：`require`、模块系统
- 用 `http.createServer` 写一个最简单的服务器
- 处理 GET/POST 请求
- 类比：Node.js ≈ Python 的 http.server

### 第 22 课：引入 Express
- 痛点：原生写路由很繁琐（if-else 地狱）
- Express 路由系统
- 中间件概念
- 重构 HTTP 服务器

### 第 23 课：文件存储用户数据
- 用 JSON 文件存储用户信息
- 实现注册接口（POST /register）
- 实现登录接口（POST /login）
- 为什么文件存储不够用（并发问题）

### 第 24 课：引入 SQLite 数据库
- 什么是数据库？为什么需要它？
- SQL 基础：CREATE TABLE、INSERT、SELECT
- 用 `better-sqlite3` 操作 SQLite
- 将文件存储迁移到数据库

### 第 25 课：Session 与 Cookie
- 什么是 Session？什么是 Cookie？
- 登录成功后下发 Session ID
- 前端保存 Cookie，后续请求自动携带
- 实现"需要登录"的接口保护

### 第 26 课：前端接入用户系统
- 注册 / 登录页面
- 登录后保存 token，显示用户名
- 游戏结束后记录胜负到数据库
- 查看历史战绩页面

---

# 阶段五：AI 对手（4 课时）

### 第 27 课：随机 AI
- 最简单的 AI：完全随机选择
- 封装 AI 接口：`makeAIMove(gameState)`
- 在终端中测试 AI vs 随机

### 第 28 课：贪心策略 AI
- 评估函数：什么是"好"的操作？
- 优先买最贵的卡、优先拿稀有宝石
- 实现简单贪心策略
- 对比随机 AI 的胜率

### 第 29 课：Web 中集成 AI
- 在 Web 界面添加"单人 vs AI"模式
- AI 操作时展示思考过程
- AI 自动执行，玩家等待

### 第 30 课：进阶 AI 策略
- Minimax 搜索算法
- 剪枝优化
- 评估函数改进
- 留作课后探索

---

# 阶段六：在线联机（6 课时）

### 第 31 课：从轮询开始
- 什么是轮询？为什么需要它？
- 实现"查询对方操作"的 API
- 每 2 秒轮询一次，获取最新状态
- 痛点：延迟高、浪费带宽

### 第 32 课：引入 WebSocket
- WebSocket 是什么？和 HTTP 的区别
- 用原生 `ws` 库建立 WebSocket 连接
- 实现双向实时通信
- 类比：HTTP ≈ 寄信，WebSocket ≈ 打电话

### 第 33 课：房间系统
- 什么是"房间"？为什么需要？
- 实现创建房间、加入房间
- 房间内两个玩家的状态同步
- 处理玩家断线重连

### 第 34 课：联机对战流程
- 玩家 A 创建房间 → 获得房间码
- 玩家 B 输入房间码加入
- 双方轮流操作，实时同步
- 胜利判定和结束画面

### 第 35 课：匹配机制
- 随机匹配 vs 好友邀请
- 实现匹配队列
- WebSocket 推送匹配结果

### 第 36 课：联机测试与优化
- 多窗口联机测试
- 延迟优化、冲突处理
- 安全性考虑（防止作弊）

---

# 阶段七：部署上线（2 课时）

### 第 37 课：部署到服务器
- 购买云服务器、配置域名
- 用 PM2 管理 Node 进程
- Nginx 反向代理
- HTTPS 证书配置

### 第 38 课：CI/CD 与监控
- 自动化部署流程
- 错误日志收集
- 性能监控
- 后续迭代方向

---

> 📝 **学习建议**
> - 每节课后，尝试不看代码自己复现一遍
> - 遇到不懂的概念，先记下来继续往下走，不要卡住
> - 每个阶段结束后，回顾该阶段学到了什么
> - 代码是你的，随时可以修改和探索
